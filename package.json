{
  "name": "@4s1/some-library",
  "version": "6.0.2",
  "description": "Some library",
  "bugs": {
    "url": "https://gitlab.com/4s1/playground/some-library/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/4s1/playground/some-library.git"
  },
  "license": "MIT",
  "author": "Steffen <steffen@4s1.de>",
  "type": "module",
  "exports": "./dist/index.js",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "build:dev": "pnpm run build -- --project tsconfig.dev.json",
    "format": "prettier --write src/",
    "preinstall": "npx only-allow pnpm",
    "lbt": "npm run lint && npm run build && npm run test",
    "lint": "echo no linting",
    "lint:fix": "npm run lint -- --fix",
    "prepare": "husky install",
    "release": "git diff --exit-code --quiet && pnpm run lbt && standard-version",
    "release:major": "pnpm run release -- --release-as major",
    "release:minor": "pnpm run release -- --release-as minor",
    "release:patch": "pnpm run release -- --release-as patch",
    "start": "node dist/index.js",
    "start:dev": "node --loader ts-node/esm src/index.ts",
    "test": "echo no tests"
  },
  "prettier": "@4s1/eslint-config",
  "dependencies": {
    "@4s1/toolbox": "3.0.0"
  },
  "devDependencies": {
    "@4s1/eslint-config": "3.2.0",
    "@4s1/ts-config": "1.2.2",
    "@commitlint/cli": "13.2.1",
    "@commitlint/config-conventional": "13.2.0",
    "@types/node": "14.17.25",
    "eslint": "8.0.0",
    "husky": "7.0.2",
    "prettier": "2.4.1",
    "standard-version": "9.3.1",
    "typescript": "4.4.4"
  },
  "publishConfig": {
    "@4s1:registry": "https://gitlab.com/api/v4/projects/28705297/packages/npm/"
  }
}
