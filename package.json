{
  "name": "@4s1/some-library",
  "version": "3.6.1",
  "description": "Some library",
  "bugs": {
    "url": "https://gitlab.com/4s1/playground/some-library/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/4s1/playground/some-library.git"
  },
  "license": "MIT",
  "author": "Steffen <steffen@4s1.de>",
  "main": "index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "echo Hello World && mkdir dist && touch dist/foo",
    "test": "echo no tests",
    "lbt": "npm run lint && npm run build && npm run test",
    "lint": "echo no linting",
    "release": "pnpm run lbt && standard-version",
    "release:major": "pnpm run lbt && standard-version --release-as major",
    "release:minor": "pnpm run lbt && standard-version --release-as minor",
    "release:patch": "pnpm run lbt && standard-version --release-as patch",
    "prepare": "husky install"
  },
  "dependencies": {
    "@commitlint/cli": "^13.1.0",
    "@commitlint/config-conventional": "^13.1.0"
  },
  "devDependencies": {
    "@4s1/semantic-release-config": "2.1.0",
    "@types/node": "14.17.15",
    "husky": "^7.0.0",
    "standard-version": "9.3.1"
  },
  "publishConfig": {
    "@4s1:registry": "https://gitlab.com/api/v4/projects/28705297/packages/npm/",
    "access": "public"
  }
}
