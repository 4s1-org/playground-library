stages:
  - foo

bar:
  stage: foo
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^reenovate\/.*$/
      when: on_success
    - when: never
  script:
    - echo $CI_COMMIT_BRANCH
    - echo $CI_COMMIT_REF_NAME
